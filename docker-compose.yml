version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: tracktern
      POSTGRES_PASSWORD: tracktern_password
      POSTGRES_DB: tracktern_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
